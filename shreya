generate key pair by code in site
then to clone a file to git use 
git clone "ssh link"
create new file by touch
use following code
git add .
git status
git commit -m "any note if you want"
git push
now if you commit changes by github site 
you will have to pull it in terminal
by using git pull
